<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        @page {
            margin: 1.5cm 2cm;
        }
        body {
            font-family: DejaVu Sans, sans-serif;
            font-size: 10pt;
            line-height: 1.5;
            color: #2c3e50;
        }
        .header {
            text-align: center;
            margin-bottom: 25px;
            padding: 20px;
            background-color: #2c3e50;
            color: white;
            border-radius: 8px;
            border-bottom: 4px solid #667eea;
        }
        h1 {
            margin: 0 0 8px 0;
            font-size: 28pt;
            font-weight: bold;
            letter-spacing: 1px;
        }
        .subtitle {
            font-size: 11pt;
            opacity: 0.95;
        }
        .info-grid {
            display: table;
            width: 100%;
            margin-bottom: 20px;
            border: 2px solid #e0e0e0;
            border-radius: 6px;
            background-color: #f8f9fa;
        }
        .info-row {
            display: table-row;
        }
        .info-label {
            display: table-cell;
            padding: 10px 15px;
            font-weight: bold;
            background-color: #495057;
            color: white;
            width: 30%;
        }
        .info-value {
            display: table-cell;
            padding: 10px 15px;
            background-color: #ffffff;
        }
        .month-section {
            page-break-inside: avoid;
            margin-bottom: 20px;
            border: 1px solid #e0e0e0;
            border-radius: 6px;
            overflow: hidden;
        }
        .month-header {
            background-color: #2c3e50;
            color: white;
            padding: 12px 15px;
            font-size: 14pt;
            font-weight: bold;
            letter-spacing: 0.5px;
            border-bottom: 3px solid #667eea;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th {
            background-color: #f1f3f5;
            padding: 10px 8px;
            text-align: left;
            font-weight: bold;
            font-size: 9pt;
            color: #495057;
            border-bottom: 2px solid #667eea;
        }
        td {
            padding: 10px 8px;
            border-bottom: 1px solid #e9ecef;
            font-size: 9pt;
        }
        tr:last-child td {
            border-bottom: none;
        }
        tr:hover {
            background-color: #f8f9fa;
        }
        .date-col {
            font-weight: bold;
            color: #667eea;
        }
        .child-col {
            font-weight: 600;
            color: #2c3e50;
        }
        .parent-col {
            color: #6c757d;
            font-size: 8.5pt;
        }
        .type-auto {
            color: #28a745;
            font-size: 8pt;
        }
        .type-manual {
            background-color: #fff3cd;
            color: #856404;
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 8pt;
            font-weight: 600;
        }
        .footer {
            position: fixed;
            bottom: 1cm;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 8pt;
            color: #adb5bd;
            padding-top: 10px;
            border-top: 1px solid #dee2e6;
        }
        .page-number:after {
            content: counter(page);
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🍳 Kochplan {{ kitaYear.yearString }}</h1>
        <div class="subtitle">
            Kita-Jahr: {{ kitaYear.startDate|date('d.m.Y') }} bis {{ kitaYear.endDate|date('d.m.Y') }}
        </div>
    </div>

    <div class="info-grid">
        <div class="info-row">
            <div class="info-label">📅 Zeitraum</div>
            <div class="info-value">{{ kitaYear.startDate|date('d. F Y') }} - {{ kitaYear.endDate|date('d. F Y') }}</div>
        </div>
        <div class="info-row">
            <div class="info-label">📊 Kochdienste gesamt</div>
            <div class="info-value">{{ totalAssignments }} Termine</div>
        </div>
        <div class="info-row">
            <div class="info-label">📄 Erstellt am</div>
            <div class="info-value">{{ "now"|date('d.m.Y um H:i') }} Uhr</div>
        </div>
    </div>

    {% for month in assignmentsByMonth %}
    <div class="month-section">
        <div class="month-header">📆 {{ month.name }}</div>
        
        <table>
            <thead>
                <tr>
                    <th style="width: 15%;">Datum</th>
                    <th style="width: 13%;">Tag</th>
                    <th style="width: 25%;">Familie (Kind)</th>
                    <th style="width: 37%;">Erziehungsberechtigte</th>
                    <th style="width: 10%; text-align: center;">Typ</th>
                </tr>
            </thead>
            <tbody>
                {% for assignment in month.assignments %}
                <tr>
                    <td class="date-col">{{ assignment.assignedDate|date('d.m.Y') }}</td>
                    <td>
                        {% set day = assignment.assignedDate|date('l') %}
                        {% if day == 'Monday' %}Mo
                        {% elseif day == 'Tuesday' %}Di
                        {% elseif day == 'Wednesday' %}Mi
                        {% elseif day == 'Thursday' %}Do
                        {% elseif day == 'Friday' %}Fr
                        {% elseif day == 'Saturday' %}Sa
                        {% elseif day == 'Sunday' %}So
                        {% endif %}
                    </td>
                    <td class="child-col">{{ assignment.party.childrenNames }}</td>
                    <td class="parent-col">{{ assignment.party.parentNames|join(', ') }}</td>
                    <td style="text-align: center;">
                        {% if assignment.isManuallyAssigned %}
                            <span class="type-manual">✏️ M</span>
                        {% else %}
                            <span class="type-auto">✓</span>
                        {% endif %}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endfor %}

    <div class="footer">
        Seite <span class="page-number"></span> | Kochdienst-Plan {{ kitaYear.yearString }} | Erstellt: {{ "now"|date('d.m.Y, H:i') }} Uhr
    </div>
</body>
</html>
