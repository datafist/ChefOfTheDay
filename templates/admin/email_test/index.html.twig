{% extends 'base.html.twig' %}

{% block title %}E-Mail-Test{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">📧 E-Mail-Test</h1>

    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Test-E-Mail versenden</h5>
                    <p class="card-text">
                        Senden Sie eine Test-E-Mail, um Ihre SMTP-Konfiguration zu überprüfen.
                        <strong>Keine echten Benachrichtigungen werden versendet.</strong>
                    </p>

                    <form method="post">
                        <div class="mb-3">
                            <label for="test_email" class="form-label">E-Mail-Adresse</label>
                            <input 
                                type="email" 
                                class="form-control" 
                                id="test_email" 
                                name="test_email"
                                value="{{ test_email }}"
                                placeholder="ihre-email@example.com"
                                required
                            >
                            <div class="form-text">
                                Geben Sie Ihre E-Mail-Adresse ein, an die die Test-E-Mail gesendet werden soll.
                            </div>
                        </div>

                        <button type="submit" class="btn btn-primary">
                            📤 Test-E-Mail senden
                        </button>
                        <a href="{{ path('admin_dashboard') }}" class="btn btn-secondary">
                            Zurück
                        </a>
                    </form>
                </div>
            </div>

            <div class="card mt-4">
                <div class="card-body">
                    <h5 class="card-title">📋 SMTP-Konfiguration</h5>
                    <p>Die E-Mail-Konfiguration erfolgt über die Umgebungsvariable <code>MAILER_DSN</code> in der Datei <code>.env.local</code>.</p>
                    
                    <h6 class="mt-3">Beispiele:</h6>
                    <pre class="bg-light p-3"><code># Gmail (App-Passwort erforderlich!)
MAILER_DSN=gmail+smtp://ihre-email@gmail.com:app-passwort@default

# GMX
MAILER_DSN=smtp://ihre-email@gmx.de:passwort@mail.gmx.net:587?encryption=tls

# Kasserver
MAILER_DSN=smtp://ihre-email@ihre-domain.de:passwort@mail.kasserver.com:465?encryption=ssl

# Eigener SMTP-Server
MAILER_DSN=smtp://benutzer:passwort@mail.ihre-domain.de:587?encryption=tls

# Kein Versand (Entwicklung)
MAILER_DSN=null://null</code></pre>

                    <div class="alert alert-info mt-3">
                        <strong>💡 Tipp:</strong> Nach dem Ändern der <code>MAILER_DSN</code> müssen Sie den Cache leeren:
                        <code>php bin/console cache:clear</code>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">ℹ️ Hinweise</h5>
                    
                    <h6 class="mt-3">Was wird getestet?</h6>
                    <ul class="small">
                        <li>SMTP-Verbindung</li>
                        <li>Authentifizierung</li>
                        <li>E-Mail-Versand</li>
                        <li>HTML-Formatierung</li>
                    </ul>

                    <h6 class="mt-3">Häufige Fehler:</h6>
                    <ul class="small">
                        <li><strong>Connection refused:</strong> SMTP-Server nicht erreichbar oder falscher Port</li>
                        <li><strong>Authentication failed:</strong> Falsche Zugangsdaten</li>
                        <li><strong>null://null:</strong> MAILER_DSN nicht konfiguriert</li>
                    </ul>

                    <h6 class="mt-3">Gmail-Nutzer:</h6>
                    <p class="small">
                        Erstellen Sie ein <strong>App-Passwort</strong>:<br>
                        Google Konto → Sicherheit → 2-Faktor-Authentifizierung → App-Passwörter
                    </p>

                    <h6 class="mt-3">E-Mail nicht erhalten?</h6>
                    <ul class="small">
                        <li>Spam-Ordner prüfen</li>
                        <li>5 Minuten warten</li>
                        <li>E-Mail-Adresse korrekt?</li>
                        <li>Logs prüfen: <code>var/log/dev.log</code></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
