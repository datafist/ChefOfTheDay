# Production Environment Variables
# Kopiere diese Datei als .env.prod und passe die Werte an

# ===== Symfony =====
APP_ENV=prod
APP_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# ===== Database =====
MYSQL_ROOT_PASSWORD=CHANGE_ME_STRONG_ROOT_PASSWORD
MYSQL_DATABASE=kochdienst
MYSQL_USER=kochdienst
MYSQL_PASSWORD=CHANGE_ME_STRONG_USER_PASSWORD

# ===== Domain =====
# Passe die Domain an deine Subdomain an
TRUSTED_HOSTS=^localhost|kochdienst\.florianbirkenberger\.de$

# ===== Mailer (optional) =====
# Für SMTP Konfiguration:
# MAILER_DSN=smtp://user:password@smtp.example.com:587

# ===== Generierung sicherer Passwörter =====
# APP_SECRET:         openssl rand -hex 32
# MYSQL_ROOT_PASSWORD: openssl rand -base64 24
# MYSQL_PASSWORD:      openssl rand -base64 24
